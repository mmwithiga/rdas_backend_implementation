apiVersion: v1
kind: Secret
metadata:
  name: rdas-secrets
  namespace: rdas
type: Opaque
stringData:
  # Replace with actual Redis password before applying
  # To encode: echo -n 'your-password' | base64
  REDIS_PASSWORD: ""
